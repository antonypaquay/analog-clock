{
  "name": "analog-clock",
  "version": "1.0.0",
  "description": "Analog Clock in Javascript",
  "main": "index.js",
  "scripts": {
    "start": "live-server --port=3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Antony Paquay",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
